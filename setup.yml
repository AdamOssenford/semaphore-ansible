---
- hosts: localhost
  gather_facts: false
  user: root
  vars:
    ansible_path: "{{ lookup('env','ANSIBLE_WORK_DIR') }}"
  tasks:
  - name: random command
    shell: /bin/hostname
    register: host_name
    
  - debug: msg="The hostname of the system is {{ host_name.stdout }}"

